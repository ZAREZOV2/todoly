# TODO Development Platform

Функциональный сайт для управления задачами разработки с авторизацией, системой ролей и прав доступа, отслеживанием статусов, комментариями и историей изменений.

## 🚀 Быстрый старт

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd todo
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env и установите NEXTAUTH_SECRET
```

4. Инициализируйте базу данных:
```bash
npx prisma db push
npx prisma db seed
```

5. Запустите dev сервер:
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Дефолтные учетные данные

- Email: `admin@example.com`
- Password: `admin123`

## 🛠️ Технологии

- **Frontend:** Next.js 15, React 19, Tailwind CSS, dnd-kit
- **Backend:** Next.js API Routes, NextAuth.js v5
- **Database:** SQLite (локально), PostgreSQL (для продакшена)
- **ORM:** Prisma
- **State Management:** Zustand

## 📋 Функции

- ✅ Авторизация и регистрация
- ✅ Система ролей и прав доступа
- ✅ Управление задачами (CRUD)
- ✅ Kanban доска с drag-and-drop
- ✅ Комментарии к задачам
- ✅ Фильтрация и поиск
- ✅ Админ-панель для управления пользователями и ролями

## 🔐 Роли и права

- **Admin** - полный доступ
- **Manager** - управление задачами и комментариями
- **Developer** - работа с назначенными задачами
- **Viewer** - только просмотр

## 📝 Скрипты

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка для продакшена
- `npm run start` - запуск продакшен сервера
- `npm run db:push` - синхронизация схемы БД
- `npm run db:seed` - заполнение начальными данными
- `npm run db:studio` - открыть Prisma Studio

## 🌐 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Установите переменные окружения:
   - `DATABASE_URL` - URL вашей PostgreSQL базы данных
   - `NEXTAUTH_SECRET` - случайная строка (можно сгенерировать: `openssl rand -base64 32`)
   - `NEXTAUTH_URL` - URL вашего приложения на Vercel
3. Vercel автоматически определит Next.js и выполнит сборку

## 📄 Лицензия

MIT
