# Инструкции по деплою

## 📤 Загрузка на GitHub

### Вариант 1: Через GitHub CLI (если установлен)

```bash
# Создать репозиторий на GitHub
gh repo create todo-dev-platform --public --source=. --remote=origin --push
```

### Вариант 2: Через веб-интерфейс GitHub

1. Перейдите на [github.com](https://github.com) и создайте новый репозиторий:
   - Название: `todo-dev-platform` (или любое другое)
   - Публичный или приватный - на ваш выбор
   - НЕ добавляйте README, .gitignore или лицензию (они уже есть)

2. После создания репозитория выполните команды:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/todo-dev-platform.git

# Загрузите код
git push -u origin main
```

### Вариант 3: Через SSH

```bash
# Добавьте remote через SSH
git remote add origin git@github.com:YOUR_USERNAME/todo-dev-platform.git

# Загрузите код
git push -u origin main
```

## 🚀 Деплой на Vercel

### Автоматический деплой через GitHub

1. Перейдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите "Add New Project"
3. Выберите ваш репозиторий `todo-dev-platform`
4. Vercel автоматически определит настройки Next.js
5. **ВАЖНО: Добавьте переменные окружения:**

   - `DATABASE_URL` - URL вашей PostgreSQL базы данных
     - Можно использовать [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres) (бесплатный тариф)
     - Или [Supabase](https://supabase.com), [Railway](https://railway.app), или другой провайдер
   
   - `NEXTAUTH_SECRET` - сгенерируйте случайную строку:
     ```bash
     openssl rand -base64 32
     ```
   
   - `NEXTAUTH_URL` - URL вашего приложения на Vercel (будет автоматически установлен, но можно указать вручную)

6. Нажмите "Deploy"

### Настройка базы данных для продакшена

#### Вариант 1: Vercel Postgres (рекомендуется)

1. В панели Vercel перейдите в Storage → Create Database → Postgres
2. Скопируйте `POSTGRES_URL` из настроек базы данных
3. Используйте его как `DATABASE_URL` в переменных окружения

#### Вариант 2: Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Перейдите в Settings → Database
3. Скопируйте Connection String (URI)
4. Используйте его как `DATABASE_URL`

#### Вариант 3: Railway

1. Создайте проект на [railway.app](https://railway.app)
2. Добавьте PostgreSQL сервис
3. Скопируйте `DATABASE_URL` из переменных окружения
4. Используйте его в Vercel

### Миграция базы данных

После деплоя нужно выполнить миграции:

1. В настройках проекта Vercel перейдите в Settings → Build & Development Settings
2. Добавьте в Build Command:
   ```
   prisma generate && prisma migrate deploy && npm run build
   ```

Или выполните миграции вручную через Vercel CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Выполните миграции
vercel env pull .env.production
npx prisma migrate deploy
```

### Заполнение начальными данными

После деплоя выполните seed скрипт:

```bash
# Через Vercel CLI
vercel env pull .env.production
npx prisma db seed
```

Или добавьте в package.json скрипт для автоматического seed при деплое (опционально).

## ✅ Проверка деплоя

После успешного деплоя:

1. Откройте URL вашего приложения (например: `https://todo-dev-platform.vercel.app`)
2. Зарегистрируйтесь или войдите с дефолтными данными:
   - Email: `admin@example.com`
   - Password: `admin123`
3. Проверьте работу всех функций

## 🔧 Обновление кода

После каждого изменения кода:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Vercel автоматически пересоберет и задеплоит новую версию!
